GCEm specific documentation should go here in [Sphinx format](http://sphinx-doc.org>).

The configured theme requires the theme "sphinx_rtd_theme" which can be imported as follows:
```
  $ pip install sphinx_rtd_theme
```
